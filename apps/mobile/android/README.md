# Android native project

This directory contains the Expo-generated Android native project.

From `apps/mobile`, regenerate the Android project when needed:

```sh
npx expo prebuild --platform android
```

For normal local Android work, `npm run android` can use or regenerate the native
project as needed.
